language: crystal
script:
  - crystal spec
  - crystal tool format --check
  - crystal docs
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  project_name: ansi2html
  on:
    branch: main
  local_dir: docs
